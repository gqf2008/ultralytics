# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

[package]
name = "yolov8-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "4.2.4", features = ["derive"] }
image = { version = "0.25.2" }
imageproc = { version = "0.25.0" }
ndarray = { version = "0.16" }
ort = { version = "2.0.0-rc.5", features = [
    "cuda",
    "tensorrt",
    "download-binaries",
    "copy-dylibs",
    "half",
] }
rusttype = { version = "0.9.3" }
anyhow = { version = "1.0.75" }
regex = { version = "1.5.4" }
rand = { version = "0.8.5" }
chrono = { version = "0.4.30" }
half = { version = "2.3.1" }
dirs = { version = "5.0.1" }
ureq = { version = "2.9.1" }
ab_glyph = "0.2.29"

# RTSP 视频流处理 (使用 vcpkg 静态库)
ez-ffmpeg = { version = "0.5.6", features = ["static"] }

# 高性能图像缩放
fast_image_resize = { version = "5.3.0" }

# 2D游戏框架 (GPU加速渲染)
macroquad = "0.4"

# egui UI框架集成
egui-macroquad = "0.17"

# 线程间通信
crossbeam-channel = "0.5.15"
crossbeam-skiplist = "0.1.3"

# 高性能内存分配器 (替代系统默认分配器)
mimalloc = { version = "0.1", default-features = false }

# 剪贴板支持
arboard = "3.4"

# 全局静态变量支持
once_cell = "1.20"

# Windows系统信息查询
[target.'cfg(windows)'.dependencies]
wmi = "0.13"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 多个可执行文件
[[bin]]
name = "yolov8"            # 图片检测: cargo run --bin yolov8
path = "src/bin/yolov8.rs"

[[bin]]
name = "sentinel"         # 数字卫兵 RTSP 实时监控 (macroquad): cargo run --bin sentinel-mq --release
path = "src/bin/sentinel.rs"
